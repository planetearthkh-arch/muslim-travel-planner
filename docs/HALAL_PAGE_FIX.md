# Halal Restaurants page loading fix

The Halal Restaurants screen now remembers the planner destination and automatically starts the first destination search when the screen opens. Cuisine choices populate after results load. The marker legend badges are keyboard- and pointer-accessible status filters. The app-shell cache version is also bumped so browsers receive the updated page instead of a stale cached copy.
