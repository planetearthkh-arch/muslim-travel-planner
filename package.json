{
  "name": "muslim-travel-planner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 -d dist",
    "build": "tsc -p tsconfig.json && cp index.html dist/index.html && cp src/styles.css dist/styles.css && cp src/maplibre.css dist/maplibre.css",
    "lint": "eslint dist --ext .js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/planner.test.js"
  },
  "devDependencies": {
    "eslint": "^10.6.0",
    "serve": "^14.2.6",
    "typescript": "^6.0.3"
  }
}
